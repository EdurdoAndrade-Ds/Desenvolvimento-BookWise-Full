# Origem da API usada quando o frontend e publicado separado do backend.
# Serve como padrao do build de producao; a variavel VITE_API_URL definida no
# provedor (Render) sobrescreve este valor.
VITE_API_URL=https://desenvolvimento-bookwise-full.onrender.com
