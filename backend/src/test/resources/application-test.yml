spring:
  datasource:
    url: jdbc:h2:mem:bookwise-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

bookwise:
  jobs:
    enabled: false
