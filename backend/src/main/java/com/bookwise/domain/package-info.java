/**
 * Camada de dominio.
 *
 * <p>Contem as entidades de negocio, objetos de valor, regras de negocio e as
 * interfaces (ports) dos repositorios. Esta camada NAO deve depender de frameworks
 * (Spring, JPA, etc.) nem de nenhuma outra camada do projeto.
 */
package com.bookwise.domain;
